"""DCS Agentic Mission Editor — MCP server package.

Exposes the mission tool surface over the Model Context Protocol so any
MCP host (Claude Desktop, Claude Code, Cursor, …) can drive mission
creation without the bundled agent layer.

Entry point: ``dcs-agentic-mcp`` (console script) or
``python -m dcs_agentic mcp``.
"""
