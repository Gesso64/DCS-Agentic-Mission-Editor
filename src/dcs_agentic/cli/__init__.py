"""CLI subcommands.

NOT YET SPLIT OUT — see PLAN.md Phase 12.

Currently the CLI lives in src/dcs_agentic/__main__.py as a single
argparse entry point. Phase 12 splits it into per-subcommand modules:
build, design, edit, inspect, validate, campaign.
"""
